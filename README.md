# Custom cmake commands
- `add_source(filename)` - adds files to the project library 
- `define_test(testname, filename)` - defines new GTest test

# TODO
- Integrating presets
- Multiconfig builds
- Benchmarking
- Presets for mutliconfig builds
- checks (CPPcheck, analyzer, valgrind, callgrind, clang-tidy, iwyu, sanitazers (all 4 of them) for tests)
- vcpkg/conan?
- compiler dependent flags
- coverage reporting
- hardening
- fuzztesting
- fortify source
- stack protector
- hooks for formatting + other useful stuff

